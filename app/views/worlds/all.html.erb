<% @page_title = "Worlds" %>

<%= render partial: "shared/page_list", locals: {count: World.visible_to(current_user).uniq.count, 
                                                 names: World.visible_to(current_user).order("name ASC").pluck(:name).uniq, 
                                                 col_length: 35, 
                                                 admin: false,
                                                 view: "worlds/",
                                                 delete: "worlds/"} %>