<% @page_title = "#{@world.name}'s pages" %>
<%= render partial: "page_list", locals: {count: @page_count, pages: @pages, lengths: [@first_amount, @second_amount, @third_amount, @fourth_amount], admin: @world.is_admin?(current_user)} %>

